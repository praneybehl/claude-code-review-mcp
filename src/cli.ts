#!/usr/bin/env node
/**
 * CLI entry point for the claude-code-review-mcp server.
 * This wrapper imports and calls the main function from index.ts.
 */

import './index.js';

// The main function is automatically called from index.ts
// No additional code needed here as the index.ts handles all server initialization
